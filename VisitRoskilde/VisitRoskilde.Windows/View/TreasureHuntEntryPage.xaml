<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VisitRoskilde.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModel="using:VisitRoskilde.ViewModel"
    x:Class="VisitRoskilde.View.TreasureHuntEntryPage"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.DataContext>
            <ViewModel:TreasureHuntEntryPageViewModel/>
        </Grid.DataContext>
        <ListView HorizontalAlignment="Left" Height="240" Margin="103,163,0,0"  VerticalAlignment="Top" Width="242"  ItemsSource="{Binding HuntList, Mode=TwoWay}" SelectedItem="{Binding SelectedHunt,Mode=TwoWay}"/>
        <TextBlock HorizontalAlignment="Left" Margin="978,172,0,0" TextWrapping="Wrap" Text="{Binding SelectedHunt.Time}" VerticalAlignment="Top" Style="{StaticResource SubheaderTextBlockStyle}"/>
        <TextBlock HorizontalAlignment="Left" Margin="978,226,0,0" TextWrapping="Wrap" Text="{Binding SelectedHunt.Theme}" VerticalAlignment="Top" Style="{StaticResource SubheaderTextBlockStyle}"/>
        <TextBlock HorizontalAlignment="Left" Margin="978,288,0,0" TextWrapping="Wrap" Text="{Binding SelectedHunt.Reward}" VerticalAlignment="Top" Style="{StaticResource SubheaderTextBlockStyle}"/>
        <TextBlock HorizontalAlignment="Left" Margin="978,349,0,0" TextWrapping="Wrap" Text="{Binding SelectedHunt.Start}" VerticalAlignment="Top" Style="{StaticResource SubheaderTextBlockStyle}"/>
        <TextBlock HorizontalAlignment="Left" Margin="978,416,0,0" TextWrapping="Wrap" Text="{Binding SelectedHunt.End}" VerticalAlignment="Top" Style="{StaticResource SubheaderTextBlockStyle}"/>
        <TextBlock HorizontalAlignment="Left" Margin="978,119,0,0" TextWrapping="Wrap" Text="{Binding SelectedHunt.Topic}" VerticalAlignment="Top" Style="{StaticResource SubheaderTextBlockStyle}"/>
        <TextBlock HorizontalAlignment="Left" Margin="846,416,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="38" RenderTransformOrigin="0.636,1.642" Text="End"/>
        <TextBlock HorizontalAlignment="Left" Margin="846,377,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="38" RenderTransformOrigin="0.636,1.642" Text="Start"/>

    </Grid>
</Page>
